--½Åµ×Éú·ç
--local speed = Char:GetSpeed
function buff22102Add(buff, Char) 
	Char:SetSpeed(50)
end

function buff22102Remove(buff, Char)
	Char:SetSpeed(222)
	--ShowError(string.format("%s",speed))
end
